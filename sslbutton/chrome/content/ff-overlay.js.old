sslbutton.onFirefoxLoad = function(event) {
  document.getElementById("contentAreaContextMenu")
          .addEventListener("popupshowing", function (e){ sslbutton.showFirefoxContextMenu(e); }, false);
};

sslbutton.showFirefoxContextMenu = function(event) {
  // show or hide the menuitem based on what the context menu is on
  document.getElementById("context-sslbutton").hidden = gContextMenu.onImage;
};

window.addEventListener("load", function () { sslbutton.onFirefoxLoad(); }, false);
